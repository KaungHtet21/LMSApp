<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.khk.lmsapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MainActionBar">
        <activity android:name=".activities.PassCodeActivity"></activity>
        <activity android:name=".activities.GroupChatActivity" />
        <activity android:name=".activities.SetNameGroupActivity" />
        <activity android:name=".activities.CreateGroupActivity" />
        <activity android:name=".activities.ImageViewActivity" />
        <activity android:name=".activities.SecurityActivity" />
        <activity android:name=".activities.ManageAccountActivity" />
        <activity android:name=".activities.AccountPrivacyActivity" />
        <activity android:name=".activities.EditImageActivity" />
        <activity android:name=".activities.SettingActivity" />
        <activity android:name=".activities.ChatActivity" />
        <activity android:name=".activities.MessageActivity" />
        <activity android:name=".activities.CreateNoteActivity" />
        <activity android:name=".activities.NoteActivity" />
        <activity android:name=".activities.BookmarkActivity" />
        <activity android:name=".activities.StudentActivity" />
        <activity android:name=".activities.DetailAttActivity" />
        <activity android:name=".activities.LecturesAddActivity" />
        <activity android:name=".activities.TotalAttActivity" />
        <activity android:name=".activities.DateActivity" />
        <activity
            android:name=".activities.UserInfoActivity"
            android:theme="@style/LoginActionBar" />
        <activity android:name=".activities.PayAttListActivity" />
        <activity android:name=".activities.TeacherActivity" />
        <activity android:name=".activities.AddCourseActivity" />
        <activity android:name=".activities.MajorListActivity" />
        <activity android:name=".activities.TeacherListActivity" />
        <activity android:name=".activities.StudentListActivity" />
        <activity android:name=".activities.TeacherRegisterActivity" />
        <activity android:name=".activities.StudentRegisterActivity" />
        <activity android:name=".activities.AdminActivity" />
        <activity android:name=".activities.SignUpActivity" />
        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/LoginActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>